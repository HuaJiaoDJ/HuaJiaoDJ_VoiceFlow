# Python environment
.venv/
__pycache__/
*.pyc

# Generated by install.sh — rebuilt per machine with local paths baked in
*.app/

# Personal settings. The app recreates both from defaults on first run;
# dictionary.json in particular holds private snippets (email, signature).
config.json
dictionary.json

# Runtime
*.log
.DS_Store
